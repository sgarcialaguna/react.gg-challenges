import { experimental_useEffectEvent, useState } from "react";


export default function useCountdown(endTime: unknown, options: unknown) {
    const [count, setCount] = useState(null);

    return count;
}
